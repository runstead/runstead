{
  "name": "@runstead/runtime",
  "version": "0.0.0",
  "description": "Task execution and worker lifecycle primitives for Runstead runtimes.",
  "license": "MIT",
  "type": "module",
  "sideEffects": false,
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "rm -rf dist && tsc -p tsconfig.build.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run",
    "lint": "eslint \"src/**/*.ts\" --max-warnings=0",
    "clean": "rm -rf dist coverage"
  },
  "dependencies": {
    "@runstead/core": "workspace:*"
  }
}
