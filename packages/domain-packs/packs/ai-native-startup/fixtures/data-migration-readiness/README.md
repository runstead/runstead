# Data Migration Readiness Fixture

This fixture represents launch-stage evidence for an AI-coded product that is
adding persistent data and needs migration gates before launch.

Expected evidence:

- migration plan describes forward and backward steps
- rollback plan is executable without manual database edits
- integrity checks cover row counts and required fields
- observability captures migration duration and failure count
