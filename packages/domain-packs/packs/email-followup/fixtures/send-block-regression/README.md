# Send Block Regression Fixture

Regression fixture proving the email-followup pack remains draft-only.

Expected evidence:

- draft preview exists
- tone and factual-risk review is recorded
- human approval remains required before sending
- `email.send` and `email.reply.send` are denied
